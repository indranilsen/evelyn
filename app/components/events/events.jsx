const React = require('react');

class Events extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div>Events</div>
        );
    }
}

module.exports = Events;