'use strict';

const _ = require('lodash');

const EventService = {
    createEvent: (type, userId, data) => {

    },

    updateEvent: (eventId, data) => {

    },

    deleteEvent: (eventId) => {

    },

    getEvents: (types, offset = 0, limit = 0, search = '') => {

    }
};

module.exports = EventService;