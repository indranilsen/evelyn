'use strict';

const _ = require('lodash');

const EventService = {
    registerUser: (name, description) => {

    },

    updateUser: (userId, name, description) => {

    },

    removeUser: (userId) => {

    },

    getUsers: (offset = 0, limit = 0, search = '') => {

    }
};

module.exports = EventService;